<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>연락처 등록</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet" />
    <style>
        body {
            background: linear-gradient(135deg, #f0f4f8, #d9e2ec);
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 30px;
        }
        .form-container {
            max-width: 480px;
            width: 100%;
            padding: 40px 40px 50px;
            background-color: #fff;
            border-radius: 14px;
            box-shadow: 0 15px 40px rgba(0, 0, 0, 0.12);
        }
        h1 {
            font-weight: 700;
            margin-bottom: 10px;
            color: #1a1a1a;
            text-align: center;
        }
        label {
            font-weight: 600;
        }
        .input-group-text {
            background: #e9ecef;
            border: none;
            border-radius: 0.375rem 0 0 0.375rem;
            color: #6c757d;
            font-size: 1.1rem;
        }
        .form-control:focus {
            box-shadow: 0 0 0 0.25rem rgba(90, 103, 216, 0.25);
            border-color: #5a67d8;
        }
        small.form-text {
            color: #6c757d;
        }
        .btn-primary {
            width: 100%;
            padding: 12px;
            font-weight: 600;
            font-size: 1.1rem;
            border-radius: 10px;
            transition: background-color 0.3s ease;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
            background-color: #5a67d8;
            border: none;
        }
        .btn-primary:hover {
            background-color: #0b5ed7;
        }
    </style>
</head>
<body>

<div class="form-container shadow-sm">
    <h1>연락처 등록</h1>
    <form id="formInput" name="forminput" method="post" action="./addconfirm" novalidate>
        <div class="mb-4">
            <label for="name" class="form-label">이름 <span class="text-danger">*</span></label>
            <div class="input-group">
                <span class="input-group-text"><i class="bi bi-person-fill"></i></span>
                <input
                        type="text"
                        class="form-control"
                        id="name"
                        name="name"
                        placeholder="이름을 입력하세요"
                        required
                />
            </div>
            <small class="form-text">정확한 이름을 입력해주세요.</small>
        </div>

        <div class="mb-4">
            <label for="phoneNumber" class="form-label">전화번호 <span class="text-danger">*</span></label>
            <div class="input-group">
                <span class="input-group-text"><i class="bi bi-telephone-fill"></i></span>
                <input
                        type="tel"
                        class="form-control"
                        id="phoneNumber"
                        name="phoneNumber"
                        placeholder="010-1234-5678"
                        pattern="[0-9\-]+"
                        required
                />
            </div>
            <small class="form-text">하이픈(-)을 포함해서 입력 가능합니다.</small>
        </div>

        <div class="mb-4">
            <label for="zipNumber" class="form-label">우편번호</label>
            <div class="input-group">
                <span class="input-group-text"><i class="bi bi-geo-alt-fill"></i></span>
                <input
                        type="text"
                        class="form-control"
                        id="zipNumber"
                        name="zipNumber"
                        placeholder="12345"
                        pattern="\d{5}"
                />
            </div>
            <small class="form-text">5자리 숫자 우편번호를 입력하세요.</small>
        </div>

        <div class="mb-4">
            <label for="email" class="form-label">이메일 주소 <span class="text-danger">*</span></label>
            <div class="input-group">
                <span class="input-group-text"><i class="bi bi-envelope-fill"></i></span>
                <input
                        type="email"
                        class="form-control"
                        id="email"
                        name="email"
                        placeholder="example@example.com"
                        required
                />
            </div>
            <small class="form-text">예: example@example.com</small>
        </div>

        <button type="submit" class="btn btn-primary">
            <i class="bi bi-check-lg"></i> 확인
        </button>
    </form>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
